<div class="container">
  <h2 class="title-lg">
    Новые пользователи
  </h2>

  <div class="users-box">
    <% @users.each do |user| %>
      <%= render partial: '_users/user_avatar', locals: {user: user} %>
    <% end %>
  </div>
</div>